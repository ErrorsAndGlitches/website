= README

== Set Up

There are two things that need to be configured to get the database and scripts functioning:
* Rails Database Access: (config/database.yml) there is a template file at config/database.yml.example
* Script Database population: (scripts/mysql_config.json) there is a template file at config/mysql_config.template.json
